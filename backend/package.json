{
  "name": "@car-tracker/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/placeholder.js",
  "types": "dist/placeholder.d.ts",
  "scripts": {
    "dev": "tsx watch src/placeholder.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build": "tsc -p tsconfig.build.json"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "@vitest/coverage-v8": "^5.0.1",
    "tsx": "^4.23.13",
    "vitest": "^5.0.1"
  }
}
